package com.hotel.repository;

import com.hotel.model.RecieptType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RecieptTypeRepository extends JpaRepository<RecieptType, Long> {
}
